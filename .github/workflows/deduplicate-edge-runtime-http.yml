name: Deduplicate Edge runtime HTTP helpers

on:
  push:
    branches: [main]
    paths:
      - scripts/deduplicate-edge-runtime-http.mjs
      - .github/workflows/deduplicate-edge-runtime-http.yml

permissions:
  contents: write

jobs:
  deduplicate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: '24.18.1'
          cache: npm

      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Install dependencies
        run: npm ci

      - name: Guarded transform and validation
        id: guarded
        shell: bash
        run: |
          set +e
          : > /tmp/runtime-http.log
          EDGE_FILES=(
            supabase/functions/admin-delete-user/index.ts
            supabase/functions/admin-retire-user/index.ts
            supabase/functions/admin-user-lifecycle/index.ts
            supabase/functions/bulk-sync-profile-phones/index.ts
            supabase/functions/auth-health-check/index.ts
          )

          NO_COLOR=1 deno check --node-modules-dir=auto "${EDGE_FILES[@]}" > /tmp/deno-before.log 2>&1
          baseline_deno_rc=$?

          node scripts/deduplicate-edge-runtime-http.mjs >> /tmp/runtime-http.log 2>&1
          transform_rc=$?
          build_rc=99
          deno_rc=99
          audit_rc=99

          if [ "$transform_rc" -eq 0 ]; then
            npm run build >> /tmp/runtime-http.log 2>&1
            build_rc=$?
          fi

          if [ "$transform_rc" -eq 0 ] && [ "$build_rc" -eq 0 ]; then
            NO_COLOR=1 deno check --node-modules-dir=auto "${EDGE_FILES[@]}" supabase/functions/_shared/runtimeHttp.ts > /tmp/deno-after.log 2>&1
            after_deno_rc=$?
            python3 - <<'PY' >> /tmp/runtime-http.log 2>&1
          import collections
          import re
          from pathlib import Path
          def diags(path):
              text = Path(path).read_text(errors='replace')
              p = re.compile(r'TS(\d+) \[ERROR\]: ([^\n]+).*?at (?:file://)?[^\n]*?/(supabase/functions/[^:\n]+):\d+:\d+', re.S)
              return collections.Counter((f,c,re.sub(r'\s+',' ',m).strip()) for c,m,f in p.findall(text))
          before = diags('/tmp/deno-before.log')
          after = diags('/tmp/deno-after.log')
          new = after - before
          print('deno before=', sum(before.values()), 'after=', sum(after.values()))
          if new:
              print('NEW_DENO_DIAGNOSTICS')
              for item,count in sorted(new.items()): print(count,item)
              raise SystemExit(1)
          PY
            deno_rc=$?
            echo "baseline_deno_rc=$baseline_deno_rc after_deno_rc=$after_deno_rc comparison_rc=$deno_rc" >> /tmp/runtime-http.log
          fi

          if [ "$transform_rc" -eq 0 ] && [ "$build_rc" -eq 0 ] && [ "$deno_rc" -eq 0 ]; then
            node scripts/audit-dead-code.mjs >> /tmp/runtime-http.log 2>&1
            audit_exec_rc=$?
            python3 - <<'PY' >> /tmp/runtime-http.log 2>&1
          import json
          from pathlib import Path
          r=json.loads(Path('scripts/.dead-code-audit.json').read_text())
          if r['production_unreachable'] != ['src/vite-env.d.ts']:
              print(r['production_unreachable'])
              raise SystemExit(1)
          PY
            audit_assert_rc=$?
            if [ "$audit_exec_rc" -eq 0 ] && [ "$audit_assert_rc" -eq 0 ]; then audit_rc=0; else audit_rc=1; fi
          fi

          if [ "$transform_rc" -ne 0 ] || [ "$build_rc" -ne 0 ] || [ "$deno_rc" -ne 0 ] || [ "$audit_rc" -ne 0 ]; then
            git reset --hard HEAD
            {
              echo "VALIDATION_FAILED"
              echo "transform_rc=$transform_rc"
              echo "build_rc=$build_rc"
              echo "deno_rc=$deno_rc"
              echo "audit_rc=$audit_rc"
              echo
              tail -n 350 /tmp/runtime-http.log
            } > scripts/.runtime-http-dedup-status.txt
            echo "success=false" >> "$GITHUB_OUTPUT"
          else
            rm -f scripts/.runtime-http-dedup-status.txt
            echo "success=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Commit validated source changes
        if: steps.guarded.outputs.success == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add supabase/functions
          git commit -m "cleanup: centralize Edge runtime HTTP helpers"
          git push origin HEAD:main

      - name: Commit failure evidence
        if: steps.guarded.outputs.success != 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add scripts/.runtime-http-dedup-status.txt
          git commit -m "chore: record runtime HTTP dedup failure" || exit 0
          git push origin HEAD:main
