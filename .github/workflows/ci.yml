name: CI (temporarily held for scoped validation)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hold:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Scoped validation in progress; normal CI restored by the validator.'
