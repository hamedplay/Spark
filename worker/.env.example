# Avatar Worker Environment Variables
# Copy this file to a secure location OUTSIDE the repository (e.g., /opt/avatar-worker/.env)
# and fill in the real values. Never commit the real .env file.

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
AVATAR_WORKER_ID=
